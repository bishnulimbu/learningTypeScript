
const enum placement {
  winner = "won",
  runnerUp = 0,
  looser
}

// placement.forEach((element) => {
//   console.log(element);
//
// });
// no forEach loop as there in no enum in js and foreach needs values to iterate not key pairs.

console.log(placement.winner);
console.log(placement.runnerUp);


export { };
